name: GitHub Actions Demo
on: [push]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo $GITHUB_OUTPUT
      - run: echo 1>>$GITHUB_OUTPUT
      - run: echo $GITHUB_OUTPUT
